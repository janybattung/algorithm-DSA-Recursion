function strSpiltter(str, separator) {
    if (!str.includes(separator)) { 
        return [str] 
    }
  
    let a = str.indexOf(separator);
    let b = str.slice(0, a)

    return array;
  }
  console.log(strSpiltter('02/20/2020', '/'));
  